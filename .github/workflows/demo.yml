name: Hello
on: workflow_dispatch
jobs:
  build:
   runs-on: ubuntu-latest
   steps:
     - name: hello krishna
       run: echo "Hello build"
  
  test:
   runs-on: ubuntu-latest
   needs: build
   steps:
     - name: hello krishna
       run: echo "Hello test"

  deploy:
   runs-on: ubuntu-latest
   needs: test
   steps:
     - name: hello krishna
       run: echo "Hello deploy"

     
